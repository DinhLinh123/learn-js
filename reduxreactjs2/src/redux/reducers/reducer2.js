const initialState = {
    name : 'React Training'
};

const nameReduxcer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state;

    }
}
export default nameReduxcer;